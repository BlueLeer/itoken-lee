server:
  port: 8888
spring:
  application:
    name: itoken-config
  cloud:
    config:
      # 在git上配置
      label: master # 指定分支
      server:
        git:
          uri: git@github.com:BlueLeer/itoken-lee.git
          search-paths: config-repo
          username: 251668577@qq.com
          password: WANGlE5588
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka/ # 注册中心地址